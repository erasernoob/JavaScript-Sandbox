// Array Literal
// Array Constructor
const fruits = new Array('apple', 'number')

// console.log(fruits)

const arr = [43, 33, 55, 33]

// arr.push(100)

// arr.unshift(99) // put the 99 in the 0index 

// arr.shift() // shift from the right to left(delete the 0index)

// arr.reverse()

x = arr.includes(99)

x = arr.slice(1) // start from the index 1 (arr dose not change !!!)

// does change the arr remove
// x = arr.splice(1, 2) // the second parm is the delete count
// 44 33 



console.log(arr)